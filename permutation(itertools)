


from itertools import permutations

s, k = input().split()
k = int(k)

res = list(permutations(s, k))

print(res)



from itertools import permutations


s, k = input().split()
k = int(k)


perms = permutations(sorted(s), k)


for perm in perms:
    print(''.join(perm))

